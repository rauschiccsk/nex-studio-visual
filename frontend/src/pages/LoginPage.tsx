/**
 * Login page — JWT login form. Implemented in a later feat.
 */
function LoginPage() {
  return (
    <div className="flex h-full w-full items-center justify-center bg-gray-50">
      <div className="w-full max-w-sm rounded-lg border border-gray-200 bg-white p-8 shadow-sm">
        <h1 className="mb-2 text-2xl font-semibold text-primary-700">
          NEX Studio
        </h1>
        <p className="mb-6 text-sm text-gray-600">
          Sign in to continue (login form coming soon).
        </p>
        <div className="rounded-md bg-gray-50 p-4 text-xs text-gray-500">
          Placeholder — authStore + login form will be implemented in a later
          task.
        </div>
      </div>
    </div>
  );
}

export default LoginPage;
