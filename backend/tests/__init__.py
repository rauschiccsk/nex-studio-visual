"""Backend-local tests.

Integration tests that exercise a BEHAVIOR.md workflow end-to-end against
the real FastAPI ``app`` live under ``backend/tests/integration/``. The
package sits inside the ``backend`` source tree so that it travels with
the feature code and is easy to cross-reference from the services and
routers under test.
"""
