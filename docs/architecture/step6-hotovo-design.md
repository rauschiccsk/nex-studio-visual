# NEX Studio — Akčné karty / Hotovo (STEP 6): implementačný podklad

> Detailný návrh kroku 6 — vedomé „Hotovo" (podpis Manažéra) robí verziu nasaditeľnou + príznak „Vývoj na zákazku".
> Nadväzuje na REDESIGN.md §3 krok 8, BUILD-PLAN.md krok 6, REDESIGN-SCOPE.md R9, STEP4/STEP5 design.
> Grounded v reálnom kóde (v2.0.0-dev, HEAD ~99e5c82). Prešiel revíziou; adversariálny verdikt: READY.
> Podklad pre Implementera — Implementer číta tento dokument; dispatch prompt je krátky a odkazuje sem.
> POZOR: úlohy + súbory sú z pôvodného návrhu; 4 OPRAVY z revízie NIŽŠIE sa MUSIA zapracovať do príslušných úloh.

## Po ľudsky (pre Manažéra projektu)

Po Kontrole sa v paneli Plán úloh objaví tlačidlo „Označiť ako hotové" — kliká ho MANAŽÉR (nie partner). Kliknutím
sa verzia uzavrie ako dokončená (terminálny stav 'done') a stane sa NASADITEĽNOU: podpis Manažéra sa zapíše viazaný
na presný stav kódu (git commit / HEAD SHA). Nasadenie (UAT/PROD obrazovky, neprerábame ich) verziu odteraz ponúkne.
Je to čestné: keď kód po podpise zmeníš, verzia automaticky vypadne z nasaditeľných (hotovo_drift), presne ako pri
starom Auditorovom PASS. Kontrola zostáva neviditeľná pre nasadenie; podpis Manažéra NAHRÁDZA verdikt (žiadny verdikt
sa nekriesi). Lišta fáz dostane 5. koncovú fázu „Hotovo". Pri zakladaní projektu pribudne prepínač „Vývoj na zákazku"
(custom_development_enabled) — len uložený údaj, jediná vec čo neskôr dovolí odchýliť sa od jednotného dizajnu.
Všetko aditívne; stará (Auditorova) cesta nasadenia ostáva bajt po bajte nezmenená.

## Rozhodnutia (Manažér projektu 2026-07-05)

- **MD-1 = (A)** Hotovo = SHA-viazaný podpis Manažéra; `version_verified` ho prijme cez novú vetvu; `deploy.py` BEZ zmeny. (B verdikt-naoko a C frozen-flag zamietnuté.)
- **MD-2 = (A) Hotovo je TERMINÁLNY podpis** (ako starý Auditorov `schvalit`→done). Po `done` `determine_available_actions` vráti `set()` — driftnutá hotovo verzia NEMÁ z UI re-podpis (`overit_znovu` vyžaduje `sha_drift`, hotovo dáva `hotovo_drift`). ČESTNE priznané, znovu-overenie pre rozhovorový tok je MIMO STEP 6 (alebo Manažér spraví novú verziu). Žiadne falošné „prirodzené znovu-otvorenie".
- **MD-3 = (A)** stavový prúžok pri hotovej rozhovorovej verzii = „Hotovo — pripravené na nasadenie" (zelené).
- **MD-4 = (A)** tlačidlo „Označiť ako hotové" = primárny štýl (konzistencia s ostatnými rozhodnými akciami).
- **MD-5 = (A) príznak „Vývoj na zákazku" len pri založení** (nemenný potom — ako `type`/`auth_mode`, NIE v ProjectUpdate).

## OPRAVY Z ADVERSARIÁLNEJ KONTROLY (musia sa zapracovať)

- MAJOR MD-2: :498-499 done returns set() unconditionally -> Hotovo TERMINAL like Auditor schvalit->done (:7126-7130); drift -> hotovo_drift drops out but NO UI re-sign (overit_znovu :7183 needs sha_drift != hotovo_drift). Accepted, out of scope; no natural re-open claim.
- MINOR-2: DeployMatrixPage at frontend/src/components/deploy/DeployMatrixPage.tsx NOT pages/; dropped Over-znova-button framing (overit_znovu unwired, 0 renderers).
- MINOR-3: guardian_enabled IS in ProjectUpdate :238 + update() :262 (updatable); clone only Column shape + Create/Read; immutability precedent is type/auth_mode (excluded :194).
- MINOR-4: HonestStatusStrip statusText :18 returns Volny first; new conversation-done branch MUST precede :18 else shadowed; no tone change (done green :133).

## Backend

HOTOVO ACTION (orchestrator.py). Deploy dnes číta deploy.list_verified_versions (deploy.py:186-211) — DVOJOSOVÝ filter: (1) kandidát = Version JOIN PipelineState WHERE current_stage=='done' (VERIFIED_STAGE, deploy.py:65,204); (2) realita = version_verified(db,vid,head)[0] True (deploy.py:211). Konverzačná stavba po Kontrole sedí na current_stage='priprava'/status='awaiting_manazer'/mode='conversation' (completion tail orchestrator.py:6592-6609), Kontrola zostáva na priprava a emituje kind='gate_report' (_run_conversation_kontrola_round) — takže PADÁ na oboch osiach a je neviditeľná pre deploy. STEP 6 to zatvára takto:

(1) Register verb: pridať "hotovo" do _ACTIONS (orchestrator.py:411-448). NIE do _ADVANCING_ACTIONS (:454-462) — je to terminálny podpis Manažéra, nie phase-walk.

(2) Offer (state-only): v determine_available_actions priprava bloku (:519-533) actions.add("hotovo") BEZPODMIENEČNE (presne ako zostav_plan/spustit_stavbu/skontrolovat). Jemné DB preconditiony sú v board post-filtri a v apply_action guarde.

(3) apply_action('hotovo') branch — NOVÝ blok umiestnený vedľa skontrolovat (:7042-7078), pred schvalit (:7080). Guard-ladder (raise ako skontrolovat): state.mode!='conversation' → raise; not spec_approved(db,version_id) (:653) → raise; not kontrola_done(db,version_id) (:630-650) → raise; state.current_stage=='done' (už hotovo) → raise. Potom SHA-anchor presne ako verdict path (:7347-7357): proj_root = claude_agent.PROJECTS_ROOT / _project_slug_for_version(db,version_id) (:680); hotovo_sha = _repo_head(proj_root) (:2972); ak hotovo_sha → _vnum = version_number; _git_tag_version(proj_root,_vnum,hotovo_sha) (:2989, best-effort -f). Zapísať PRÁVE JEDNU správu cez _record_message (sole writer :766): stage='priprava', author='manazer', recipient='ai_agent', kind='notification', content plain-SK, payload={"phase":"priprava","hotovo":True, plus "hotovo_sha":hotovo_sha ak existuje}. KRITICKÉ: kind='notification' (NIE 'done', NIE 'verdict'; NIE stage='verifikacia') — 'notification' je v MESSAGE_KIND_VALUES (pipeline.py:76-89), používa ho už completion tail (:6602), a je NEVIDITEĽNÉ pre _verifikacia_passed (číta len stage='verifikacia' ∧ kind∈{verdict,return}, :1779-1791). Terminálne usadenie (zrkadlo schvalit done-branch :7126-7130): state.current_stage='done'; state.current_actor='ai_agent' (terminál, platná ACTOR hodnota); state.status='done'; state.next_action="Verzia je hotová — nasadenie (UAT/PROD) je samostatný krok."; db.flush(); return state. ŽIADNY _begin_dispatch — hotovo NIE je dispatchnutý agent turn (na rozdiel od skontrolovat/zostav_plan ktoré volajú _begin_dispatch a routujú cez run_conversation_turn na durable marker); je to čistý terminálny podpis, takže run_conversation_turn NEpotrebuje hotovo-delegačnú vetvu.

(4) hotovo_done(db,version_id) probe — NOVÝ, zrkadlo kontrola_done (:630-650): latest stage='priprava' ∧ kind='notification' ∧ payload.hotovo (JSONB .astext=='true') seq. Pre honest-by-construction „už hotovo": hotovo je re-otvorené keď fresh programming_complete (_latest_programming_complete_seq :602-618) má vyššie seq než hotovo marker — takže guard „already-hotovo" = current_stage=='done' (stačí, lebo terminálny stav sám blokuje), a re-open po novej stavbe funguje cez kontrola_done→skontrolovat→znovu hotovo ladder.

(5) version_verified reconciliation (:1794-1835) — NOVÁ vedúca vetva PRED _verifikacia_passed gate: _manazer_signoff(db,version_id) helper (vedľa _verifikacia_passed :1764) číta latest stage='priprava' ∧ kind='notification' ∧ payload.hotovo správu a vráti jej payload (alebo None); STALE-aware ako _verifikacia_passed CR-V2-055 — ak _latest_programming_complete_seq má vyššie seq než signoff → None (re-build po hotove). V version_verified: signoff = _manazer_signoff(...); if signoff is not None: pass_sha = signoff.get("hotovo_sha") a použiť ROVNAKÝ SHA-anchor ladder ako verdict path (:1826-1835): žiadne sha → (True,'hotovo_unbound'); head is None → (True,'repo_unreadable'); pass_sha==head → (True,'hotovo_match'); inak → (False,'hotovo_drift'). else: EXISTUJÚCA verdict vetva NEZMENENÁ (legacy stavby nemajú hotovo marker → signoff None → prepadnú byte-identicky do if not _verifikacia_passed(...): return False,'no_pass'). head batch arg (:1809) rešpektovaný — žiadny git subprocess per verzia na deploy liste.

(6) Board post-filter (pipeline.py, zrkadlo skontrolovat bloku :148-158): dropni 'hotovo' pokiaľ NIE (state.mode=='conversation' AND spec_approved (:107 local) AND orchestrator.kontrola_done(...) AND state.current_stage!='done'). verified_provenance (:175-187) už nesie nový 'hotovo_*' string do FE.

FLAG (viď deploy_and_flag).

## Frontend

HOTOVO RUNG (PlanUlohRail.tsx). Ladder je mutually-exclusive if/else reťaz (:322-395) hnaná board.available_actions, každý rung volá runTrigger(action,failMsg) (:281-293) → postPipelineActionApi(versionId,{action}) → onBoard. Pridať: (a) canFinish odvodený flag vedľa canCheck (:275): const canFinish = !!board?.available_actions?.includes("hotovo"); (b) NOVÝ `else if (canFinish)` blok pripojený PO canCheck rungu (:395, aby bol POSLEDNÝ rung — BE ho ponúka len po Kontrole), skopírovaný z canCheck rungu (:380-394): intro text „Kontrola prebehla — keď si spokojný, označ verziu ako hotovú."; button label idle „Označiť ako hotové" / busy „Označujem…"; runTrigger("hotovo","Označenie ako hotové zlyhalo."). Styling: primary bg-primary-600 ako ostatné rozhodné Manažér rungy (UX konzistencia). (c) Voliteľne (MD: plain hint): keď board?.state?.status==='done' && board?.state?.mode==='conversation', v (inak prázdnom) ladder slote statická veta „Verzia je hotová. Nasadenie (UAT/PROD) je samostatný krok — nájdeš ho v ľavom menu." (nie toast, nie cross-domain tlačidlo).

ACTION UNION (pipeline.ts:123-137): pridať `| "hotovo"` s STEP-6 komentárom, vedľa skontrolovat (:137). Ručne písaná request union (NIE codegen z pipeline.generated.ts) — žiadna regen pre samotný verb. verified_provenance je už na PipelineBoardRead; FE ho môže voliteľne použiť na odlíšenie „hotovo_*" od Auditorovho „sha_match".

TERMINAL HOTOVO PHASE (PhaseBar.tsx). Konverzačný strip (:39-74) odvodzuje phase z board signálov, evaluated furthest-right-first (dnes rightmost = kontrola :63). Pridať 'hotovo' ako 5. terminálnu fázu: do ConvPhase type (:42), CONV_PHASES pole (:43), CONV_LABELS (:44-49) s labelom „Hotovo". V conversationPhase() pridať PRVÚ (najpravejšiu) kontrolu: `if (board?.state?.status === "done") return "hotovo";` — done konverzačná stavba označí Hotovo ako current a spätne označí Kontrolu ✓. convMarkFor (:76-82) generalizuje cez CONV_PHASES indexy → funguje s 5 fázami bez zmeny. Legacy strip (STRIP_PHASES filtruje 'done', :21) NEZMENENÝ — mení sa len mode==='conversation' vetva (:92).

STATUS STRIP (HonestStatusStrip.tsx — MD, odporúčané). statusText (:17-26) dnes status==='done' → „Voľný". Pridať conversation-aware vetvu: keď state.status==='done' && state.mode==='conversation' → „Hotovo — pripravené na nasadenie" so zeleným tónom (číta state.mode, už na PipelineState pipeline.ts:61). Legacy „Voľný" pre non-conversation done nezmenené.

DEPLOY MATRIX: ŽIADNA FE zmena — DeployMatrixPage.tsx (:105,196,277-302) už renderuje matrix.verified_versions do Nasadiť dropdownu; keď BE hotovo pridá verziu do list_verified_versions, objaví sa automaticky. UAT/PROD obrazovky sa NEPRERÁBAJÚ (OSTÁVA). Sidebar UAT/PROD nav (:239-254) už dosiahnuteľný.

FLAG FE (viď deploy_and_flag).

## Nasadenie + príznak

DEPLOY RECONCILIATION (net zero-line change v deploy.py). deploy.list_verified_versions (deploy.py:186-211) sa NEMENÍ: candidate os (current_stage=='done', :204) je splnená hneď ako hotovo terminálne usadí verziu na current_stage='done' (presne ako Auditorova cesta cez schvalit→done); reality os (version_verified :211) teraz vráti True pre hotovo-podpísanú verziu cez novú _manazer_signoff vetvu. Legacy (Auditorova) deploy cesta zostáva byte-identická — non-conversation stavby nemajú hotovo marker, version_verified prepadne do pôvodnej _verifikacia_passed/verified_sha logiky. SHA-drift auto-un-verify: hotovo_sha vs HEAD → 'hotovo_drift' → verzia ticho vypadne z list_verified_versions (žiadny frozen podpis), zrkadlo CR-V2-056 pre verdict. „Over znova" pre driftnutú hotovo verziu sa NEBUDDUJE — re-otvorí sa prirodzene (nová stavba/oprava → fresh programming_complete → kontrola_done False → Kontrola znovu → Hotovo znovu → čerstvý SHA-viazaný podpis re-anchoruje).

custom_development_enabled FLAG (R9 / BUILD-PLAN step 6) — čisto aditívne, klon guardian_enabled precedentu:
- Model (backend/db/models/projects.py:62, vedľa guardian_enabled): custom_development_enabled = Column(Boolean, nullable=False, server_default="false").
- Migrácia migrations/versions/081_project_custom_development_enabled.py (down_revision "080"; latest je 080): kopíruj 079/080 idempotentný tvar — upgrade: op.execute("ALTER TABLE projects ADD COLUMN IF NOT EXISTS custom_development_enabled BOOLEAN NOT NULL DEFAULT false"); downgrade: op.execute("ALTER TABLE projects DROP COLUMN IF EXISTS custom_development_enabled"). ŽIADNA úprava drift-testu netreba: tests/test_alembic_migrations.py (:103 test_no_schema_drift_vs_models, :167 test_alembic_upgrade_head_on_clean_database) používa compare_metadata proti Base.metadata (nie ručný column-list), takže správne zmatchovaná migrácia+ORM stĺpec drží testy zelené automaticky.
- Schémy (backend/schemas/project.py): ProjectCreate (:148 pattern) custom_development_enabled: bool = Field(default=False, description=...); ProjectRead (:267 pattern) custom_development_enabled: bool. ProjectUpdate ZÁMERNE VYNECHANÝ (flag fixnutý pri založení, ako type/auth_mode — R9).
- Service (backend/services/project.py:205-221): prepošli custom_development_enabled=data.custom_development_enabled do Project(...) konštruktora (vedľa guardian_enabled=data.guardian_enabled :218). update() field-set (:252-263) sa NEMENÍ.
- FE typy (frontend/src/types/project.ts): ProjectCreate (:57-63, po enable_branch_protection) custom_development_enabled?: boolean; ProjectRead (:101, po guardian_enabled) custom_development_enabled: boolean.
- FE NewProjectPage.tsx: const [customDevelopment,setCustomDevelopment]=useState(false) vedľa F-004 flagov (:77-79); custom_development_enabled: customDevelopment do createProjectApi payload (:191-194); checkbox „Vývoj na zákazku (povoľuje odchýliť sa od jednotného firemného dizajnu)" v „Možnosti nastavenia" bloku (:439-471) zrkadliaci enable_cicd (:444-452). createProjectApi berie ProjectCreate — pridanie poľa do typu stačí.

Flag je INERTNÝ údaj v STEP 6 — žiadne správanie sa naň neviaže (deviation gate je budúci scope).

## Súbory (16)
- `modify` `/opt/projects/nex-studio/backend/services/orchestrator.py` — Add 'hotovo' to _ACTIONS (:411-448, NOT _ADVANCING). actions.add('hotovo') state-only in determine_available_actions priprava block (:519-533). New apply_action('hotovo') branch beside skontrolovat (:7042), before schvalit (:7080): guard-ladder (mode=='conversation'/spec_approved/kontrola_done/current_stage!='done'); SHA-bind via _repo_head(:2972)+_git_tag_version(:2989); record ONE _record_message(:766) stage='priprava' author='manazer' recipient='ai_agent' kind='notification' payload{hotovo:True,hotovo_sha}; terminal settle current_stage='done'/status='done'/current_actor='ai_agent', NO _begin_dispatch (mirror schvalit done-branch :7126-7130). New hotovo_done(db,version_id) probe (mirror kontrola_done :630-650). New _manazer_signoff(db,version_id) helper beside _verifikacia_passed(:1764): latest priprava∧notification∧payload.hotovo, stale if newer programming_complete seq(:602-618). version_verified(:1794): leading signoff branch reusing the SAME SHA-anchor ladder(:1826-1835) BEFORE the _verifikacia_passed gate; legacy path byte-identical when signoff is None.
- `modify` `/opt/projects/nex-studio/backend/api/routes/pipeline.py` — Board post-filter for 'hotovo' (mirror skontrolovat block :148-158): drop unless state.mode=='conversation' AND spec_approved(:107 local) AND orchestrator.kontrola_done AND state.current_stage!='done'. verified_provenance(:175-187) already carries the new 'hotovo_*' string.
- `modify` `/opt/projects/nex-studio/backend/services/deploy.py` — NO code change — list_verified_versions(:186-211) candidate axis (current_stage=='done' :204) is satisfied by hotovo terminal settle, reality axis version_verified(:211) now honours the manager signoff. Body stays byte-identical; add/keep a test asserting the conversation version is included. (Listed for traceability; expected diff = 0 lines.)
- `modify` `/opt/projects/nex-studio/backend/db/models/projects.py` — Add custom_development_enabled = Column(Boolean, nullable=False, server_default='false') beside guardian_enabled(:62).
- `create` `/opt/projects/nex-studio/migrations/versions/081_project_custom_development_enabled.py` — down_revision '080'. Idempotent copy of 079/080: upgrade ALTER TABLE projects ADD COLUMN IF NOT EXISTS custom_development_enabled BOOLEAN NOT NULL DEFAULT false; downgrade DROP COLUMN IF EXISTS. No drift-test edit needed (compare_metadata is metadata-derived).
- `modify` `/opt/projects/nex-studio/backend/schemas/project.py` — ProjectCreate(:148 pattern) add custom_development_enabled: bool = Field(default=False, description=...); ProjectRead(:267 pattern) add custom_development_enabled: bool. ProjectUpdate NOT touched (set at creation).
- `modify` `/opt/projects/nex-studio/backend/services/project.py` — create()(:205-221) pass custom_development_enabled=data.custom_development_enabled into Project(...) beside guardian_enabled(:218). update() field-set unchanged.
- `modify` `/opt/projects/nex-studio/frontend/src/services/api/pipeline.ts` — Add | "hotovo" to the PipelineActionName union (:123-137) with a STEP-6 comment. Hand-written union — no codegen regen for the verb.
- `modify` `/opt/projects/nex-studio/frontend/src/components/riadiace/PlanUlohRail.tsx` — Add canFinish flag beside canCheck(:275); append new else-if(canFinish) rung AFTER the canCheck rung(:395) copied verbatim: intro 'Kontrola prebehla — keď si spokojný, označ verziu ako hotovú.', button 'Označiť ako hotové'/'Označujem…', runTrigger('hotovo',...). Optional: when status==='done' && mode==='conversation' render a static plain hint in the empty ladder slot pointing to UAT/PROD in the sidebar.
- `modify` `/opt/projects/nex-studio/frontend/src/components/riadiace/PhaseBar.tsx` — Add 'hotovo' as a 5th terminal ConvPhase + CONV_PHASES + CONV_LABELS('Hotovo'). In conversationPhase() add a FIRST check `if (board?.state?.status === 'done') return 'hotovo';`. Legacy strip untouched (only mode==='conversation' branch :92).
- `modify` `/opt/projects/nex-studio/frontend/src/components/riadiace/HonestStatusStrip.tsx` — statusText(:17-26): for status==='done' && state.mode==='conversation' read 'Hotovo — pripravené na nasadenie' (green tone) instead of bare 'Voľný'; reads state.mode (already on PipelineState). Legacy 'Voľný' for non-conversation done unchanged.
- `modify` `/opt/projects/nex-studio/frontend/src/types/project.ts` — ProjectCreate(:57-63) add custom_development_enabled?: boolean; ProjectRead(:101) add custom_development_enabled: boolean. ProjectUpdate not touched.
- `modify` `/opt/projects/nex-studio/frontend/src/pages/NewProjectPage.tsx` — Add customDevelopment useState beside F-004 flags(:77-79); add custom_development_enabled: customDevelopment to createProjectApi payload(:191-194); add 'Vývoj na zákazku' checkbox in 'Možnosti nastavenia' block(:439-471) mirroring enable_cicd(:444-452).
- `create` `/opt/projects/nex-studio/tests/test_conversation_hotovo.py` — Backend test (TEST DB :9178, mirror tests/test_conversation_kontrola.py + smoke stub): hotovo offered ONLY on conversation+spec_approved+kontrola_done+not-done; apply_action raises per precondition; records kind='notification' stage='priprava' payload.hotovo+hotovo_sha and NOT a verifikacia verdict (_verifikacia_passed stays False); state settles current_stage='done'/status='done' with no dispatch; version_verified True (hotovo_match) and deploy.list_verified_versions INCLUDES it; HEAD move past signed sha → version_verified False (hotovo_drift) → excluded; fresh programming_complete re-opens Kontrola+Hotovo; legacy Auditor verdict path + deploy byte-identical (no hotovo marker → verdict path).
- `modify` `/opt/projects/nex-studio/frontend/src/__tests__/components/test_PlanUlohRail.test.tsx` — Add 'Hotovo trigger (STEP 6)' describe: rung offered ONLY on available_actions:['hotovo'], fires postPipelineActionApi(v1,{action:'hotovo'})→onBoard, and is the LAST rung (canCheck wins over hotovo if both offered).
- `modify` `/opt/projects/nex-studio/frontend/src/__tests__/components/test_PhaseBar.test.tsx` — Add cases: done conversation board (status:'done', mode:'conversation') marks 'Hotovo' current and 'Kontrola' done (✓); legacy strip still renders exactly the 4 legacy phases (no 'Hotovo' leak).

## Úlohy pre Implementera (poradie)
1. **custom_development_enabled — ORM column + migration 081**
   - Add custom_development_enabled = Column(Boolean, nullable=False, server_default="false") to Project (backend/db/models/projects.py) directly beside guardian_enabled (:62). Create migrations/versions/081_project_custom_development_enabled.py with down_revision="080"; copy the idempotent shape of 079/080: upgrade op.execute("ALTER TABLE projects ADD COLUMN IF NOT EXISTS custom_development_enabled BOOLEAN NOT NULL DEFAULT false"); downgrade op.execute("ALTER TABLE projects DROP COLUMN IF EXISTS custom_development_enabled"). Do NOT edit any drift test — it is metadata-derived.
   - Súbory: `/opt/projects/nex-studio/backend/db/models/projects.py`, `/opt/projects/nex-studio/migrations/versions/081_project_custom_development_enabled.py`
   - Overenie: TEST_DATABASE_URL on :9178: run `python -m pytest tests/test_alembic_migrations.py -q` — test_migration_files_form_contiguous_chain, test_no_schema_drift_vs_models, test_alembic_upgrade_head_on_clean_database all PASS (column present, zero drift).
2. **custom_development_enabled — Pydantic schemas + create service**
   - backend/schemas/project.py: add custom_development_enabled: bool = Field(default=False, description="Vývoj na zákazku — the only switch permitting deviation from the unified default design (firemné zásady §4). Default False.") to ProjectCreate (:148 pattern), and custom_development_enabled: bool to ProjectRead (:267 pattern). Do NOT touch ProjectUpdate. backend/services/project.py create(): pass custom_development_enabled=data.custom_development_enabled into the Project(...) constructor beside guardian_enabled (:218). Leave update() field-set unchanged.
   - Súbory: `/opt/projects/nex-studio/backend/schemas/project.py`, `/opt/projects/nex-studio/backend/services/project.py`
   - Overenie: `python -m pytest tests/schemas/test_project.py tests/test_project_router.py backend/tests/integration/test_workflow_create_project.py -q` PASS; a created project round-trips custom_development_enabled False by default and True when set.
3. **custom_development_enabled — FE type + NewProjectPage checkbox**
   - frontend/src/types/project.ts: add custom_development_enabled?: boolean to ProjectCreate (after enable_branch_protection :63) and custom_development_enabled: boolean to ProjectRead (after guardian_enabled :101). frontend/src/pages/NewProjectPage.tsx: add const [customDevelopment,setCustomDevelopment]=useState(false) beside the F-004 flags (:77-79); add custom_development_enabled: customDevelopment to the createProjectApi payload (:191-194); add a checkbox in the 'Možnosti nastavenia' block (:439-471) mirroring enable_cicd (:444-452), label 'Vývoj na zákazku (povoľuje odchýliť sa od jednotného firemného dizajnu)'.
   - Súbory: `/opt/projects/nex-studio/frontend/src/types/project.ts`, `/opt/projects/nex-studio/frontend/src/pages/NewProjectPage.tsx`
   - Overenie: cd frontend && npm run type-check && npm run build succeed; the New Project form renders the new checkbox (unchecked default).
4. **Hotovo action — register verb + state-only offer + board post-filter**
   - orchestrator.py: add "hotovo" to _ACTIONS (:411-448); do NOT add to _ADVANCING_ACTIONS. In determine_available_actions priprava block (:519-533) add actions.add("hotovo") unconditionally (state-only), mirroring skontrolovat, with a STEP-6 comment. backend/api/routes/pipeline.py: add a post-filter mirroring the skontrolovat block (:148-158) — drop 'hotovo' unless state.mode=='conversation' AND the existing spec_approved local (:107) AND orchestrator.kontrola_done(db,version_id) AND state.current_stage!='done'. Reuse the spec_approved local. No apply_action handler yet (added in T5) — until then hotovo is offered but not applicable; keep T4+T5 in ONE commit so the app is never runnable with an offerable-but-unhandled action.
   - Súbory: `/opt/projects/nex-studio/backend/services/orchestrator.py`, `/opt/projects/nex-studio/backend/api/routes/pipeline.py`
   - Overenie: Combined with T5. `python -m pytest tests/test_conversation_kontrola.py -q` still PASS (no regression to the STEP-5 offer path).
5. **Hotovo action — apply_action handler + terminal settle + probes**
   - orchestrator.py: add hotovo_done(db,version_id) probe mirroring kontrola_done (:630-650) — latest stage='priprava' ∧ kind='notification' ∧ payload.hotovo (.astext=='true') seq. Add the apply_action('hotovo') branch beside skontrolovat (:7042), BEFORE schvalit (:7080): guard-ladder (state.mode!='conversation' → raise OrchestratorError; not spec_approved → raise; not kontrola_done → raise; state.current_stage=='done' → raise 'už hotovo'). Then SHA-anchor exactly like the verdict path (:7347-7357): proj_root = claude_agent.PROJECTS_ROOT / _project_slug_for_version(db,version_id); hotovo_sha = _repo_head(proj_root); if hotovo_sha: _vnum from Version; _git_tag_version(proj_root,_vnum,hotovo_sha). Record ONE _record_message: stage='priprava', author='manazer', recipient='ai_agent', kind='notification', content plain-SK, payload={'phase':'priprava','hotovo':True} plus 'hotovo_sha' if set. Terminal settle (mirror schvalit done-branch :7126-7130): current_stage='done'; current_actor='ai_agent'; status='done'; next_action='Verzia je hotová — nasadenie (UAT/PROD) je samostatný krok.'; db.flush(); return state. NO _begin_dispatch. Commit T4+T5 together.
   - Súbory: `/opt/projects/nex-studio/backend/services/orchestrator.py`
   - Overenie: Ad-hoc on :9178: an offered hotovo applies → records one kind='notification' payload.hotovo message (NOT a verifikacia verdict), _verifikacia_passed stays False, state.current_stage=='done'/status=='done', no dispatch fired. Precondition violations raise OrchestratorError.
6. **version_verified — manager signoff branch (deploy-eligibility)**
   - orchestrator.py: add _manazer_signoff(db,version_id)->Optional[dict] beside _verifikacia_passed (:1764): read the latest stage='priprava' ∧ kind='notification' ∧ payload.hotovo message; STALE-aware — if _latest_programming_complete_seq (:602-618) is newer than that message's seq, return None; else return its payload. In version_verified (:1794) add a leading branch BEFORE the _verifikacia_passed gate: signoff=_manazer_signoff(...); if signoff is not None: pass_sha=signoff.get('hotovo_sha'); apply the SAME ladder as the verdict path (:1826-1835): no sha → (True,'hotovo_unbound'); if head is None resolve via _repo_head; head None → (True,'repo_unreadable'); pass_sha==head → (True,'hotovo_match') else (False,'hotovo_drift'). else: fall through to the unchanged _verifikacia_passed/verdict path. deploy.py stays byte-identical.
   - Súbory: `/opt/projects/nex-studio/backend/services/orchestrator.py`
   - Overenie: `python -m pytest tests/ backend/tests/ -q -k 'verifikacia or deploy or version_verified or workflow_deploy_matrix'` PASS — legacy verdict path + deploy matrix unchanged; a hotovo-signed conversation version reports version_verified True 'hotovo_match'.
7. **Backend test — tests/test_conversation_hotovo.py**
   - Mirror tests/test_conversation_kontrola.py scaffolding (TEST DB :9178, smoke stub if needed). Cover: (1) hotovo offered ONLY when conversation+spec_approved+kontrola_done+current_stage!='done'; refused otherwise (apply_action raises per precondition); (2) marker is stage='priprava' kind='notification' payload.hotovo+hotovo_sha, and _verifikacia_passed stays False (NOT a verdict); (3) terminal settle current_stage='done'/status='done', no dispatch; (4) monkeypatch _repo_head so hotovo_sha==head → version_verified True 'hotovo_match' AND deploy.list_verified_versions includes the version_number; HEAD moved → 'hotovo_drift' → excluded; (5) fresh programming_complete re-opens kontrola_done+hotovo; (6) legacy (mode NULL) build has no hotovo marker → version_verified keys off the verdict path unchanged.
   - Súbory: `/opt/projects/nex-studio/tests/test_conversation_hotovo.py`
   - Overenie: `python -m pytest tests/test_conversation_hotovo.py -q` PASS.
8. **FE — Hotovo rung + action union + hint**
   - frontend/src/services/api/pipeline.ts: add | "hotovo" to PipelineActionName (:137) with a STEP-6 comment. frontend/src/components/riadiace/PlanUlohRail.tsx: add const canFinish = !!board?.available_actions?.includes("hotovo") beside canCheck (:275); append a new else-if(canFinish) rung AFTER the canCheck rung (:395), copied verbatim, intro 'Kontrola prebehla — keď si spokojný, označ verziu ako hotovú.', button 'Označiť ako hotové'/'Označujem…', runTrigger('hotovo','Označenie ako hotové zlyhalo.'), primary styling. Optional static hint in the empty ladder slot when state.status==='done' && state.mode==='conversation' pointing to UAT/PROD in the sidebar.
   - Súbory: `/opt/projects/nex-studio/frontend/src/services/api/pipeline.ts`, `/opt/projects/nex-studio/frontend/src/components/riadiace/PlanUlohRail.tsx`
   - Overenie: cd frontend && npm run type-check && npm run build succeed; the rung renders only when available_actions includes 'hotovo'.
9. **FE — terminal Hotovo phase + status strip**
   - frontend/src/components/riadiace/PhaseBar.tsx: add 'hotovo' to ConvPhase (:42), CONV_PHASES (:43), CONV_LABELS (:44-49, label 'Hotovo'); in conversationPhase() add a FIRST check `if (board?.state?.status === "done") return "hotovo";`. Leave the legacy strip (STRIP_PHASES :21) and phaseMarkFor untouched. frontend/src/components/riadiace/HonestStatusStrip.tsx: in statusText, for state.status==='done' && state.mode==='conversation' return 'Hotovo — pripravené na nasadenie' (green tone via the existing tone plumbing); keep bare 'Voľný' for non-conversation done.
   - Súbory: `/opt/projects/nex-studio/frontend/src/components/riadiace/PhaseBar.tsx`, `/opt/projects/nex-studio/frontend/src/components/riadiace/HonestStatusStrip.tsx`
   - Overenie: cd frontend && npm run type-check && npm run build succeed; a done conversation board marks 'Hotovo' current + 'Kontrola' ✓; legacy strip unchanged.
10. **FE tests — PlanUlohRail + PhaseBar**
   - Extend frontend/src/__tests__/components/test_PlanUlohRail.test.tsx: hotovo rung offered ONLY when available_actions:['hotovo'], fires postPipelineActionApi(v1,{action:'hotovo'})→onBoard, and is LAST (canCheck wins if both offered). Extend frontend/src/__tests__/components/test_PhaseBar.test.tsx: done conversation board marks 'Hotovo' current + 'Kontrola' ✓; legacy strip still exactly 4 phases (no 'Hotovo' leak).
   - Súbory: `/opt/projects/nex-studio/frontend/src/__tests__/components/test_PlanUlohRail.test.tsx`, `/opt/projects/nex-studio/frontend/src/__tests__/components/test_PhaseBar.test.tsx`
   - Overenie: cd frontend && npx vitest run src/__tests__/components/test_PlanUlohRail.test.tsx src/__tests__/components/test_PhaseBar.test.tsx PASS.

## Overenie

BACKEND UNIT (TEST DB :9178 ONLY — conftest.py SAVEPOINT + _guard_prod_db_isolation; NEVER PROD :9198). Per-task: T1 `python -m pytest tests/test_alembic_migrations.py -q` (contiguous chain + zero drift + clean-DB upgrade — the metadata-derived drift test stays green with no edit). T2 `python -m pytest tests/schemas/test_project.py tests/test_project_router.py backend/tests/integration/test_workflow_create_project.py -q`. T5/T6 ad-hoc + targeted `-k 'verifikacia or deploy or version_verified or workflow_deploy_matrix'`. T7 `python -m pytest tests/test_conversation_hotovo.py -q` covering: offer preconditions (conversation+spec_approved+kontrola_done+not-done); apply_action raises per precondition; marker is stage='priprava' kind='notification' payload.hotovo (NOT a verifikacia verdict → _verifikacia_passed False); terminal settle current_stage='done'/status='done', no dispatch; version_verified True 'hotovo_match' + deploy.list_verified_versions INCLUDES the version; HEAD drift → 'hotovo_drift' → excluded; fresh programming_complete re-opens; legacy verdict path + deploy byte-identical.

FULL PYTEST (mandatory — orchestrator.version_verified + deploy.list_verified_versions are SHARED, consumed by test_workflow_deploy_matrix + the verifikacia tests; the Implementer self-verify rule requires the whole suite on a shared-module change): `python -m pytest -q` against :9178. Confirm the new version_verified signoff branch does not regress the verdict path and no test outside STEP 6 changes behaviour. Cross-check greppable artifacts: `grep -rl 'version_verified\\|list_verified_versions\\|hotovo' tests backend/tests` and run any matches.

FRONTEND (PROD nginx static bundle — project_nex_studio_fe_prod_build; verify with type-check+build+vitest, NO docker up, NEVER PROD :9197/:9198): `cd frontend && npm run type-check && npm run build && npx vitest run`. New/updated specs: test_PlanUlohRail.test.tsx (hotovo rung offered-only + fires hotovo + LAST after canCheck), test_PhaseBar.test.tsx (done conversation → Hotovo current + Kontrola ✓; legacy strip 4 phases byte-identical). PipelineActionName is hand-written (no codegen regen needed for the verb); confirm no pipeline.generated.ts drift-gate is tripped.

APP-RUNNABLE-AFTER-EACH: commit T4+T5 together (offer + handler in one commit so an offerable action is always handled); every other task is independently additive and leaves the app runnable. Legacy (mode NULL) deploy path and STEP 1-5 conversation flow remain byte-identical throughout.